(ns brain-fuel.project-euler.cli.main-test
  (:require [clojure.test :refer [deftest testing is]]
            [brain-fuel.project-euler.cli.main :as sut]))

(deftest a-test
  (testing "FIXME, I do nothing."
    (is (= 0 0))))
